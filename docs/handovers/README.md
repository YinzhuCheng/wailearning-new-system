# Handovers (Legacy)

This directory exists for legacy or in-progress handover notes that have not
yet been normalized into [`docs/handoffs/`](../handoffs/README.md).

Rules:

- prefer `docs/handoffs/` for committed, durable, user-requested handoff
  documents;
- keep links here repository-relative so documentation governance checks can
  resolve them;
- do not use absolute local filesystem paths in committed Markdown links.

Current contents:

- this README only; active durable handoffs now live under [`docs/handoffs/`](../handoffs/README.md)
